theme {
#    color_scheme = # full path to color scheme 
#    icon_theme = # name of icon thmee to use
    style = kvantum-dark # widget style
    font_fixed = monospace
    font_fixed_size = 11
    font= monospace
    font_size = 11
}

misc {
    single_click_activate = 0 # 1=activate 0=open
    menus_have_icons = 1
    shortcuts_for_context_menus = 1 # context meuns show their shortcuts
}
