### My Zshrc ###

### Enviroment vars can probably delete since handled in uwsm
#export ZSH="$HOME/.oh-my-zsh"
#export MANPATH="/usr/local/man:$MANPATH"
#export ARCHFLAGS="-arch $(uname -m)"
#source $HOME/.config/zsh/fzfinit.sh
#source $HOME/.config/zsh/mcflyinit.sh

### Plugins
plugins=(git sudo fzf git-prompt zoxide zsh-autosuggestions)

### Prompt
setopt PROMPT_SUBST
TMOUT=30
TRAPALRM() {
  zle reset-prompt
}
export PS1=" %F{blue}%D{%H:%M}  󰣇  %f%~/ $ "

### Zsh Options
CASE_SENSITIVE="true"
ENABLE_CORRECTION="false"
COMPLETION_WAITING_DOTS="true"
DISABLE_UNTRACKED_FILES_DIRTY="false"
HIST_STAMPS="dd.mm.yyyy"
HISTSIZE=100
SAVEHIST=1 # mcfly does that
HISTFILE="$HOME/Documents/logging/zsh_history"
KEYBOARD_HACK=#
unsetopt xtrace             # Prints the comands beeing executed spams stoud
unsetopt verbose            # Extra verbose log printing,
setopt notify               # Tells status changes of background jobs
setopt auto_list
setopt auto_param_keys
setopt auto_param_slash 
setopt auto_continue
setopt auto_resume
unsetopt correct
setopt extended_glob
setopt pushd_ignore_dups
setopt autocd
setopt cdable_vars
setopt print_exit_value
setopt rm_star_wait
setopt sun_keyboard_hack
setopt interactive_comments
setopt auto_menu 
setopt menu_complete
setopt long_list_jobs
setopt pipe_fail
setopt globdots
unsetopt prompt_sp
setopt hist_ignore_all_dups
setopt hist_ignore_space
setopt hist_lex_words
setopt hist_reduce_blanks
setopt hist_save_no_dups
setopt hist_reduce_blanks
unsetopt append_history             # mcfly does that i think
unsetopt inc_append_history         # mcfly does that i think
unsetopt share_history              # mcfly does that i think
# zstyle ':completion:' menu select

### Initiation of other tools
eval "$(zoxide init zsh)"
eval "$(mcfly init zsh)"
source <(fzf --zsh)

### Functions
y() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
	yazi "$@" --cwd-file="$tmp"
	if cwd="$(<"$tmp")" && [[ -n "$cwd" && "$cwd" != "$PWD" ]]; then
		builtin cd -- "$cwd"
	fi
	rm -f -- "$tmp"
}


### PNPM
#export PNPM_HOME="/home/TatenK/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac

